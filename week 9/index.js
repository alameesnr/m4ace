// main.js
const calculateRectangleArea = require('./starter');

const length = 5;
const width = 6;

const area = calculateRectangleArea(length, width);
console.log(`The area of the rectangle is: ${area}`);
